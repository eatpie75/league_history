// Generated by CoffeeScript 1.3.3
(function() {
  var LolClient, a, client, fs, options, repl, summoner, util;

  LolClient = require('./lib/lol-client');

  util = require('util');

  fs = require('fs');

  repl = require('repl');

  options = {
    region: 'na',
    username: 'unprojapina1',
    password: 'penis2',
    version: '1.63.12_07_15_18_46'
  };

  summoner = {
    name: 'eatpie75',
    account_id: 142098,
    summoner_id: 115259
  };

  client = new LolClient(options);

  a = [];

  client.on('connection', function() {
    var con;
    con = repl.start({
      'prompt': '>',
      'useGlobal': true
    });
    con.context.client = client;
    con.context.options = options;
    return con.context.summoner = summoner;
  });

  client.connect();

  module.exports = client;

}).call(this);
