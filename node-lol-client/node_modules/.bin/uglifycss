#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../uglifycss/uglifycss" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../uglifycss/uglifycss" "$@"
  ret=$?
fi
exit $ret
