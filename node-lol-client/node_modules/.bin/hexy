#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../hexy/bin/hexy_cmd.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../hexy/bin/hexy_cmd.js" "$@"
  ret=$?
fi
exit $ret
